# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: employee-service
spec:
  type: LoadBalancer
  selector:
    app: employee-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
